import { RegisterWizard } from '@/features/auth/RegisterWizard';

export const RegisterPage = () => (
  <div className="min-h-[calc(100vh-3.5rem)] flex items-center justify-center px-4 py-12 bg-paper">
    <RegisterWizard />
  </div>
);
