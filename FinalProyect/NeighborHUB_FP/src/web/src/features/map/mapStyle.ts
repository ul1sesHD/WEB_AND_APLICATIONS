/**
 * Mapbox style URL — neutral, low saturation light theme that lets the colored
 * teardrop pins stand out. Override via env in the future if desired.
 */
export const MAP_STYLE_URL = 'mapbox://styles/mapbox/light-v11';

export const DEFAULT_ZOOM = 13.5;
export const RADIUS_FILL  = 'rgba(192, 57, 43, 0.10)';
export const RADIUS_LINE  = '#C0392B';
